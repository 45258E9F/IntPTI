-# this is the sample config file for phase
-.SET
-DIR = org.sosy_lab.cpachecker.core.phase;
-.TES
-
-.DEF cfa
-.TYPE = $(DIR).CFACreatePhase
-.FED
-
-.DEF int
-.TYPE = $(DIR).SingleAlgorithmRunPhase
-phase.singleAlgorithm.analysis = config/intervalAnalysis.properties 
-.FED
-
-.DEF collect
-.TYPE = $(DIR).AnalyzingResultPhase
-.FED
-
-.RUN
-collect : int;
-int : cfa;
